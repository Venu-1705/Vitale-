# Coach platform — copy to .env and fill in. NEVER commit .env.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
# Absolute API base for the dev server (the coach app and API run on different ports).
VITE_API_URL=http://localhost:3000/api
